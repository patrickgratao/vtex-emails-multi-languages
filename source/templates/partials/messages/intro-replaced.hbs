<tr>
  <td class="w-100 ph4-ns pt3 pb4 yamamay__text">
    <p class="mt4">
      <!-- partial:hi.hbs --><!-- partial -->
      ((i18n.order.introMessageReplaced))
    </p>

    <!-- partial:order-link.hbs --><!-- partial -->

    <!-- Boleto bancário -->
    {{#each orders.0.paymentData.transactions}}
      {{#each payments}}
        {{#eq paymentSystemName 'Boleto Bancário'}}
          <p>((i18n.payment.bankInvoiceReminder))</p>
          <p>
            <a class="custom__principal-button" href="{{replace url '{Installment}' installments}}">((i18n.payment.bankInvoiceShow))</a>
          </p>
        {{/eq}}
      {{/each}}
    {{/each}}
  </td>
</tr>
